
'use client';

// This component is currently not in use as server actions throw errors
// which are caught in the component that calls them. If client-side
// Firestore operations are added later, this can be used.

export function FirebaseErrorListener() {
  return null;
}
